# Get a free key at https://aistudio.google.com/apikey
# Leave empty to run entirely in demo mode (canned sample results).
GEMINI_API_KEY=

# Force canned results even when a key is set.
# Recommended for live client pitches so the demo can never fail.
DEMO_MODE=false

PORT=3000
